<!DOCTYPE html>
<html lang="en">
<head>
    <title>Rejestracja</title>
    <link rel="stylesheet" href="/stylesheets/auth.css">
</head>
<body>
<form id="form" action="/register" method="POST">
    <h1>Zarejestruj się</h1>
    <% if (error) { %>
        <div class="error-message"><%= error %></div>
    <% } %>
    <div class="input">
        <label for="login">Login</label>
        <input id="login" name="login" type="text" placeholder="Login" value="<%= typeof login !== 'undefined' ? login : '' %>" required>
    </div>
    <div class="input">
        <label for="password">Hasło</label>
        <input id="password" name="password" type="password" placeholder="Hasło" required>
    </div>
    <div class="input">
        <label for="password2">Powtórz hasło</label>
        <input id="password2" name="password2" type="password" placeholder="Hasło" required>
    </div>
    <button type="submit">Zarejestruj</button>
</form>
<p>Masz konta? <a href="/login">Zaloguj się</a></p>

</body>
</html>
