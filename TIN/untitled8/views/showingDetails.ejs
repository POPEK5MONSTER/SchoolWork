<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Szczegóły Seansu</title>
    <link rel="stylesheet" href="/stylesheets/showingDetails.css">
</head>
<body>
<h1><%= showing.movie_title %></h1>
<ul class="details-list">
    <li><strong>Gatunek:</strong> <%= showing.genre %></li>
    <li><strong>Czas trwania:</strong> <%= showing.duration %> minut</li>
    <li><strong>Opis:</strong> <%= showing.description %></li>
    <li><strong>Data premiery:</strong> <%= new Date(showing.release_date).toLocaleDateString() %></li>
    <li><strong>Czas seansu:</strong> <%= new Date(showing.showing_time).toLocaleString().slice(0, -3) %></li>
</ul>
<div class="reserve">
    <form style="<%= user && user.role_id === 2 ? '' : 'display:none;' %>" action="/reserve/<%= showing.showing_id %>/book" method="POST">
        <button type="submit">Rezerwuj bilet</button>
    </form>
</div>

<div class="back-button">
    <a href="/reserve" class="btn-back">Powrót do strony głównej</a>
</div></body>
</html>
